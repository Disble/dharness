# Learning Log (Vitácora)

A human-readable record of _why_ things in this repo are the way they are:
decisions taken and non-obvious problems solved, so future sessions inherit the
reasoning instead of rediscovering it.

This is a memory aid, **not** a substitute for deterministic guards. Whenever a
lesson can be enforced (linter, test, `go vet`, pre-commit gate), do that too —
this file only explains the _why_, it never replaces the _how_.

## How to append

- One lesson per line, kept to a single short sentence.
- Format: `- [YYYY-MM-DD]: text`
- Newest entries at the bottom. Never rewrite past entries; add a new line if
  something changes.

## Entries

- [2026-08-09]: A published binary is authority over its own documentation — react-doctor 0.9.11 rejects `--blocking`, which its own `--help` examples still advertise.
- [2026-08-09]: `npx <tool>` with no version can resolve a stale cached release: react-doctor came back as 0.2.1 while `@latest` gave 0.9.11.
- [2026-08-09]: Measure before optimising — full project detection costs 0.114 ms, so caching it would only buy a stale-cache failure mode.
- [2026-08-09]: Seam what cannot be controlled (process execution, git, cwd, build info) and use a temp directory instead of faking the filesystem.
- [2026-08-09]: Go's `flag` package stops parsing at the first positional argument, so flags written after paths are silently consumed as paths.
- [2026-08-09]: Windows cannot spawn an npm `.cmd` shim directly; it has to go through `cmd.exe`.
- [2026-08-09]: Reporting a resolved binary path instead of the tool's name makes every failure message a small puzzle.
- [2026-08-09]: StrykerJS has no `--since` — that flag belongs to Stryker.NET, and the only incremental mechanism is `--incremental` plus `--force`.
- [2026-08-09]: Stryker's `mutate` accepts `file:startLine-endLine`, but a range cannot be combined with a glob in the same entry, so the list is always computed.
- [2026-08-09]: Stryker exits 0 with surviving mutants unless `break` is set; its default is documented as "never let your build fail".
- [2026-08-09]: Stryker's `json` reporter writes a file while react-doctor's `--json` writes to stdout — structured output is not one mechanism.
- [2026-08-09]: Stryker aborts when the initial test run fails, so mutation only answers a question worth asking once the tests are green.
- [2026-08-09]: Stryker derives which tests to run from the import graph, so barrel files can inflate "related tests" until the scoping disappears.
- [2026-08-09]: Stryker's vitest runner ignores `coverageAnalysis`, always uses `perTest`, and forces single-threaded execution.
- [2026-08-09]: More workers made Stryker slower on a small scope (3.85 s with eight against 3.59 s with the default), so capping concurrency costs nothing.
- [2026-08-09]: `fallow audit` already exits 1 on a fail verdict and already gates on new findings only (`--gate new-only`).
- [2026-08-09]: `fallow audit` with no base flag resolves a sensible local base without a remote, while `--changed-since` resolves a commit range that cannot see the index.
- [2026-08-09]: fallow rejects an unknown config key by listing every valid one — the entry-point key is `entry`, not `entries`.
- [2026-08-09]: fallow reports its resolved entry points directly, so the heuristic invented to detect a misconfigured graph was never needed.
- [2026-08-09]: In fallow boundaries a type-only import still counts as a violation, while an import of an external package does not.
- [2026-08-09]: `fallow list --boundaries` prints the expanded zones with file counts and warns when a zone matches nothing — that is the configuration check.
- [2026-08-09]: gremlins produced 94%, 0% and 0% efficacy across three identical runs, so it cannot stand behind a gate.
- [2026-08-09]: Mermaid in a published artifact needs `theme: "base"` with explicit `themeVariables`, and a `<br/>` inside a label eats the surrounding space.
- [2026-08-09]: StrykerJS exposes no way to fail on survivors from the command line — neither `--break` nor `--thresholds.break` exists, so the verdict has to come from reading its report.
- [2026-08-09]: `--dryRunOnly` completes without writing any report, so the test count it produces exists only in the line of prose that states it.
- [2026-08-09]: Stryker's `cleanTempDir` only runs on a successful exit, and even set to `always` a sandbox can stay locked for a moment on Windows, so removal needs retries.
- [2026-08-09]: A tool's binary name is not its package name: `stryker` comes from `@stryker-mutator/core`, and asking a registry for the binary name fetches something else.
- [2026-08-09]: Repository-local state belongs in the git common directory: git ignores it, worktrees share it, and it dies with the repository instead of outliving it in a hashed cache.
- [2026-08-09]: lefthook and fallow both compose with `extends`, so a tool can own its own config file and the project's own file only gains one line.
- [2026-08-09]: `lefthook-local.yml` is auto-merged without touching the project's config, but it is conventionally gitignored, so it cannot carry anything meant to be shared.
- [2026-08-09]: `react-doctor install --yes` writes five things — skills for every detected agent, a package script, a dev dependency, a git hook that competes with an existing gate, and a CI workflow — and has no flag to ask for the skill alone.
- [2026-08-09]: `claude mcp add <name> <command> [args]` is not interactive, and fallow ships the `fallow-mcp` binary in its own package.
- [2026-08-09]: An ESLint rule runs unmodified as a react-doctor plugin, `messageId` and `meta.messages` included, but `sourceCode.getJSDocComment` is present and throws.
- [2026-08-09]: react-doctor rule severity accepts only `error`, `warn` or `off`, so a rule cannot carry a threshold and `context.options` arrives empty.
- [2026-08-09]: oxlint has neither `no-restricted-syntax` nor `jsdoc/require-jsdoc`, which is why shape and documentation rules cannot be expressed as configuration.
- [2026-08-09]: react-doctor does not run oxlint's core rules from `.oxlintrc.json`; adopting that config means reading severities for its own rules, not executing others.
- [2026-08-09]: fallow rejects an unknown config key by listing every valid one, and `fallow recommend --format json` classifies each decision as detected, defaulted or taste, with the question already shaped for an agent to ask.
- [2026-08-09]: In fallow's boundaries a type-only import still crosses a zone while an external package does not, and `boundary-violation` defaults to `error`.
- [2026-08-09]: A comment above `export function f() {}` belongs to the export statement, not to the function inside it, so asking the inner node finds nothing and every documented export reports as bare.
- [2026-08-09]: ESLint's `estree` types describe JavaScript only — interfaces, type aliases and enums exist solely in the AST the TypeScript parser hands over, so a rule that reads them has to name the shape it reads.
- [2026-08-09]: A rule that relies on config globs for scope reports every file in the project under react-doctor, whose rule configuration is a severity and nothing else.
- [2026-08-09]: npm returns E404 on an unauthenticated publish, so "not found" can mean "I do not know who you are" rather than anything about the package.
- [2026-08-09]: A trusted publisher cannot be configured before the package exists, so the first publish needs a token and only later releases can be OIDC-only.
- [2026-08-09]: npm validates `package.json` against the signed provenance attestation and rejects the publish when `repository.url` does not match the repository the build came from.
- [2026-08-09]: release-please cannot open its pull request until the repository allows Actions to create one, which is a repository setting rather than a workflow permission.
- [2026-08-09]: Explicit per-job `permissions:` override the repository default, so `default_workflow_permissions` can stay `read` even for a workflow that writes.
- [2026-08-09]: A pull request opened with `GITHUB_TOKEN` does not trigger workflow runs, which is why the publish job has to live inside the release workflow.
- [2026-08-09]: `tee /dev/stderr` does not exist in the Git Bash the Windows runner uses, so a check can fail on how it reports rather than on what it found.
- [2026-08-09]: The Windows runner checks out CRLF by default and gofmt counts a CRLF file as unformatted, so the format check failed on every file at once.
- [2026-08-09]: `fallow audit` cannot detect a base branch in a repository with no commits, which is exactly the first commit after adoption.
- [2026-08-09]: `git diff --cached --name-only` quotes any path that is not plain ASCII, so `src/café.ts` arrives as the literal `"src/caf\303\251.ts"` and `filepath.Ext` reads `.ts"` — the gate dropped the file and exited 0 over a change it never read. `-z` is the fix.
- [2026-08-09]: A Wails repository has two roots — git and lefthook at the top, the whole JS project in a subdirectory — and one `Root` field answered both, so a Go module reported as an npm project with no lockfile in sight.
- [2026-08-09]: `package.json` appears at every level of a workspace and a Wails project generates one nobody wrote (`frontend/wailsjs/runtime/`), so the lockfile is the signal for where a package manager installs; workspaces keep exactly one, at the root where `node_modules` lives.
- [2026-08-09]: `git ls-files` already excludes `node_modules` and honours `.gitignore`, so asking git for lockfiles needs no skip list and no depth bound, which a filesystem walk does.
- [2026-08-09]: `git rev-parse --show-toplevel` reports forward slashes on Windows while `os.Getwd` reports backslashes, and on a case-insensitive volume two spellings name one directory — comparing paths as strings after `EvalSymlinks` still gets it wrong, so identity is `os.Stat` plus `os.SameFile`.
- [2026-08-09]: lefthook has a per-command `root:` key, so a gate installed at the repository root running against a subdirectory project is one line of configuration rather than anything dharness has to solve.
- [2026-08-09]: Stryker's initial test run is where it starts its runner processes, so `--dryRunOnly` without `--concurrency` starts one per core — 19 on a 20-thread machine, and the dry run outlasted the mutation it was measuring. A resource budget does not stop applying because a run mutates nothing.
- [2026-08-10]: A dependency install belongs in init's transaction: package-manager compensation removes exactly that run's additions, then byte snapshots restore the manifest and lockfile even after a partial install failure.
- [2026-08-10]: A remote Stryker runner must share Core's transient install and be named with `--appendPlugins`; pnpm hides it from the default glob, while Yarn dlx cannot also supply the project's Vitest safely.
- [2026-08-10]: Yarn dlx isolation does not justify blocking every Yarn project: `node_modules` layouts can use npx's joint package environment, while generated PnP loaders prove dependency resolution is incompatible.
- [2026-08-10]: A zero-mutant ooze run needs a candidate preflight before `ooze.Release`, because its internal `t.Fatal` can make every later diagnostic unreachable.
- [2026-08-10]: `Delegated` moved from a compile-time type assertion on a second interface to a per-project `Step` method, closing both the rollback bug that aborted the whole run on a pre-configured `.fallowrc.json` and the unenforced Figure 1 repository stop in one contract change.
- [2026-08-10]: Asking whether a package is installed has no correct form from outside the package manager — `node_modules` is an artifact that outlives a rollback and is absent under Yarn PnP, `package.json` is a declaration nobody necessarily acted on, and a lockfile means parsing four formats — so `installStep` runs the idempotent install and reads the exit code instead.
- [2026-08-11]: A grep cannot answer "are there imports between features": a lateral import inside `src/features` never climbs through a `features/` segment, so `rg` reported zero where `fallow dead-code --boundary-violations` found 22 — the wrapped tool knew, and the shortcut invented an answer, which is the same failure as reading `node_modules` to decide whether a package is installed.
- [2026-08-11]: `folder-ownership` requires a split module to publish an `index.ts`, so a project that bans barrel files cannot ever satisfy it — measured at eight permanent findings in one such repository — which makes it an architectural choice rather than a guardrail; it now ships `off` and the architecture prompt says how to turn it on, while `pure-index-barrel` stays `error` because constraining a barrel that exists is inert, not false, where none do.
- [2026-08-11]: fallow's `extends` replaces a key rather than merging it, so a `boundaries` block in the project's own config silently discards the one dharness owns — measured against fallow 3.14.0, where the child's block won and the only output was a warning about the child's own zone; the check for it looks for the quoted `"boundaries"` rather than the bare word, because a correctly wired config carries the word in a comment explaining where boundaries live.
- [2026-08-11]: The staged mutation wrapper scored 1.00 over a suite that was already failing, because a mutant "dies" whenever the test command fails and a red baseline kills every mutant without mutating anything; the same change scored 0.83 with one real survivor once the suite was green.
- [2026-08-11]: react-doctor adopts `.eslintrc.json` and runs the rules declared there — measured on 0.5.7, a valid file reports `eslint/no-console` and exits 1 while the same file with a trailing comma reports nothing, exits 0, and names the file nowhere even under `--verbose` — and it is the only lint config adopted: `.eslintrc.cjs`, `.eslintrc.yml`, `package.json`'s eslintConfig key and flat config were each measured and ignored, so dharness checks one file rather than a family and a project on flat config has nothing to answer for.
- [2026-08-11]: `.fallowrc.json` is JSONC in practice — the motivating repository's own copy opens with a comment on line 2, and `json.Unmarshal` returns `Expecting property name enclosed in double quotes: line 2 column 3` on it — so `declaredKeys` (the generalisation of `declaresBoundaries`) stays the same textual quoted-key test rather than reaching for `encoding/json`, and is asked of `.fallowrc.jsonc` as well, matching fallow's own `fallowConfigFiles` list.
- [2026-08-11]: The region dharness rewrites inside `.dharness/fallow.jsonc` is marked `// dharness:presets begin — rewritten by \`dharness sync\`; edits here are lost.` / `// dharness:presets end` — the wording states its own machine-written nature and consequence directly in the file, so a reader needs no external doc to know a hand edit between the markers does not survive the next `sync`; this spelling ships from Slice 2 onward and is not expected to change, per design decision 8.
- [2026-08-11]: A step that can never be satisfied is a note, not a step: dharness cannot read `fallow.toml` textually because TOML keys are bare, and reporting that as an unsatisfied step would put an entry in the plan no state the project can reach ever clears (§15), so the blind spot is printed beside the plan while the answer stays "unknown, not clear" rather than an unearned "nothing collides".
- [2026-08-11]: `Detect` returning a bare `false` on a read/decode failure is indistinguishable from "not this framework at all" — a malformed `wails.json` was answering "not a Wails project" silently, the exact failure class `framework-presets` exists to end — so `preset.Match` gained an `Uncertain` field: the preset still matches, still contributes its documented default, and names what it could not read; the note surfaces beside the plan (`setup.UncertainPresetNote`, printed under `## Not checked` in `sync`'s report) rather than as an unsatisfiable step, the same reasoning `UncheckableConfigNote` already established for `fallow.toml`.
- [2026-08-11]: A path built with `filepath.Join` for evidence/comment text, not a filesystem call, renders with the OS's own separator (`\wails.json` on Windows) and breaks a golden fixture's cross-platform byte-identity the moment it is captured on one OS and compared on another; `filepath.ToSlash` is needed on any path that ends up in rendered text, separately from the path used to actually read the file.
- [2026-08-11]: Wails' own `wailsjsdir` default (`v2/pkg/commands/build/base.go`: `filepath.Join(cwd, "frontend")`) computes a different ignore pattern depending on whether Root and Source are the same directory or split — `frontend/wailsjs/**` when they coincide, `wailsjs/**` only in the split layout the motivating repository actually has — so a task list's literal example for the coincident case has to be checked against the formula, not copied from the split case's answer.
- [2026-08-11]: A preset that recognises a project but cannot read its configuration must still match, contribute the documented default, and say what it guessed from — answering "not this framework" because a file would not parse is a silent no-op, and the fix is data rather than an error because deriving the plan has to stay a pure read that cannot fail.
- [2026-08-11]: `filepath.Join` in text that reaches a golden fixture embeds the running OS's separator, so a fixture captured on Windows can never pass on Linux; evidence strings need `filepath.ToSlash` while the path used for `os.Stat` stays native.
- [2026-08-11]: fallow honours gitignore, and `.next/` and `.expo/` are gitignored by every Next.js/Expo starter — measured: an orphan file inside a gitignored `.next/` is not reported by fallow at all, the same file in a tracked directory is reported as unused — so neither preset contributes an `ignorePatterns` fact; a preset earns one only for generated code the project actually commits, which is why Wails' `wailsjs/**` is different, not an exception to the rule.
- [2026-08-11]: The design's `Manifest` shipped with only `Facts`, but the proposal asked for presets carrying facts *and* decisions — the decisions half was structural context (Next.js's documented router folders, for instance), not zones, so `Manifest` gained a second dimension, `Seeds`, rendered into `ArchitecturePrompt` as "confirm or correct this against the tree" and never as a zone; §21 still keeps zones with the agent, and Seed carries no key at all, so it cannot collide with the reserved-`boundaries` guard `Fact` already has.
- [2026-08-11]: Expo's file-based-routing documentation returned 404 during this change, so the preset ships detection-only with an empty manifest — an unverified seed would be worse than none, the same discipline `CLAUDE.md`'s second rule already applies to a rule's verdict.
- [2026-08-11]: A golden fixture that substitutes the repository path for `<root>` so it stays portable is structurally blind to an absolute path leaking into a file the project commits — found by running the built binary against a real Wails project, where the evidence comment carried the machine's own temp path, which would have made `.dharness/fallow.jsonc` differ on every checkout and, since the step compares the region's bytes, rewritten it on every run forever.
- [2026-08-11]: dharness's first cross-cutting opinion travels as a `generic` preset fact rather than as skeleton text, because fallow's `extends` replaces a key instead of merging it and only a *contributed* key joins the collision step's candidate set — a default written outside the manifest would be discarded in silence by any project declaring the same key.
- [2026-08-11]: dharness's opinions belong to the tools' configuration, not to the project's folders: `duplicates.mode`, `minOccurrences` and `threshold` are transversal because none of them needs to know the repository, while zoning does and stays with the agent (§21). The test for whether a contributed key is an opinion at all is whether it departs from the tool's own default — a key that restates one is noise every adopting repository has to maintain, which is why a defensive pin over nine fallow rules already at `error` was dropped rather than shipped.
- [2026-08-11]: A configuration key is only a gate if some command enforces it: `duplicates.threshold` is honoured by `fallow dupes` and ignored by `fallow audit`, measured at 80% duplication where audit exits 0 and dupes exits 1, so shipping the number without running dupes would have written a ceiling into every project that capped nothing.
- [2026-08-12]: Node resolves a bare module specifier by walking up from the *importing file*, never from the repository root, so a `Root/.dharness/eslint.config.js` cannot name a package installed under `Source/`. Measured, not reasoned: an owned config at `Root/.dharness/` importing `dharness-eslint-plugin` fails with `ERR_MODULE_NOT_FOUND` while the byte-identical import from `Source/` resolves. The motivating repository makes it worse than the plain case — its root carries no `package.json` and an *empty* `node_modules/`, so the walk has a plausible-looking stop that holds nothing. This is why the owned ESLint config is a factory over injected bindings rather than a plain array: the bare specifiers have to live in the project's own config, which is the file that sits beside `node_modules`.
- [2026-08-12]: `renderGolden` runs the whole plan for every fixture in the suite, so wiring a write into `ownedFilesStep.Apply` changes all five goldens at once, not only the two the slice was reasoning about — the generic pair by hand, the three framework fixtures through their `-update` path. A slice plan that names which fixtures a change touches has to count the ones nobody was looking at.
- [2026-08-12]: Deleting a step does not delete the prose that points at the file the step wrote. `doctorConfigStep` went, and `ArchitecturePrompt` kept telling the reader to turn the barrel rule on in `doctor.config.json` — with a test pinning the sentence, so the suite stayed green while the advice named a file dharness no longer writes. A step's blast radius includes every string that mentions its output, and only a search for the *file name* finds those; searching for the step's identifier does not.
- [2026-08-12]: The gate's ESLint stage placement was measured, not assumed: on a reference project (5 staged files — 4 `.ts`, 1 `.tsx` — react-doctor 0.9.11, fallow 3.15.0, eslint 10.8.1 with typescript-eslint 8.67.0), three runs each with `npx --yes <tool>@latest` for the remote two (matching production resolution) and the project's own installed binary for ESLint, the median wall-clock came out ESLint 1008 ms, fallow dupes 1398 ms, fallow audit 2102 ms, react-doctor 2959 ms — ESLint was the cheapest of the four, not the most expensive as the design's "provisional last" placement assumed, so it now runs first in the gate; react-doctor and fallow keep the relative order they already had, which this measurement did not revisit even though it measured fallow as cheaper than react-doctor here too.
- [2026-08-12]: Line 105's fix found every leftover string by searching the deleted step's *file name*, `doctor.config.json` — but `git.go:146`'s `PublishesBarrels` doc comment named the step by its *identifier*, `doctorConfigStep`, with no mention of the file anywhere in the sentence, and survived that search untouched. A step's blast radius needs both searches, one for the file it wrote and one for the step's own name; either alone misses what the other catches.
- [2026-08-12]: Three separate defects in this change were the same shape — prose asserting something the code does not do: `ArchitecturePrompt` routed to a deleted file with a test pinning the sentence; `PublishesBarrels`'s comment named a deleted step; and `EslintResidueNote`'s comment claimed it "names what it found" while the code printed a disjunction instead. The suite stayed green because tests pinned the wrong sentences. One search by *file name* and one by *identifier* together find these; either search alone misses what the other catches — this is a failure mode to watch for, not a coincidence.
- [2026-08-12]: A delta spec and a main spec are written in different tenses, and merging one into the other without translating leaves the fourth instance of the defect above. The delta said "`doctorConfigStep` MUST NOT write severities into `doctor.config.json`" — correct while the step still existed and the change was about stopping it. Merged into the spec that describes the current system, it names a step that is gone. A delta describes a transition; a main spec describes a state, and the archive step is where the first has to be rewritten into the second.
- [2026-08-12]: `structured-reports`' glyph set (`── ✓ ✗ ■ │ ·`) was measured as a slice-1 merge condition on Windows 11 (build 10.0.26200): a Go binary printing it rendered every glyph correctly at the console's default code page 437, and again after forcing `[Console]::OutputEncoding` to 437 and to 850 from PowerShell, in a no-profile PowerShell session, and in Git Bash — the glyph set is cleared and the ASCII fallback constants stay an unimplemented, recorded contingency. Honest limit: true legacy `cmd.exe` conhost could not be exercised, because `cmd` would not launch non-interactively in this environment, so this measures the modern console host at code page 437, not a genuine legacy conhost.
- [2026-08-12]: `fallow config`'s two subcommands answer different questions and only one can detect absence — measured against the reference project, a local binary took roughly 347/358/349 ms across three runs; `fallow config --path` exits 3 on a project with no fallow config at all, while `fallow config --format json` exits 0 even then, printing the fully resolved defaults, so it can never itself be used to detect absence — the probe has to be `--path`, first, with `--format json` only run after it answers 0.
- [2026-08-13]: `bunx --package A --package B binary` installs only **B**. Measured against the transient environment dharness built for Stryker: its `package.json` read `{"dependencies":{"@stryker-mutator/vitest-runner":"^9.6.1"}}` — Core, requested as `@stryker-mutator/core@latest`, was absent and arrived only as a transitive dependency at the runner's pinned range. The unit test named `TestStrykerCommandProvisionsCoreAndRunnerTogether` passed the whole time because it compared an argument list and never looked at `node_modules`. Two corollaries worth keeping: the `bunx-<digits>-` prefix is constant per machine rather than a content hash, so a changed `--package` set silently reuses a stale environment; and putting the extra package last instead makes bun fail with "does not provide a binary named stryker". npm and pnpm take repeated `--package=` properly, so this is bun's shape alone — but it was dharness's only multi-package call site.
- [2026-08-13]: Stryker's `TSConfigPreprocessor` imports `typescript` dynamically from its own location, so Core running out of a `bunx`/`npx` temporary directory cannot resolve the compiler the project installed and any project holding a `tsconfig.json` dies with `ERR_MODULE_NOT_FOUND` before its first mutant. Nothing in this repository caught it because `rg tsconfig` over the whole tree, fixtures included, returns nothing — the preprocessor had never run in a dharness test. `NODE_PATH` does not help (ESM ignores it, measured) and `--tsconfigFile` is not a CLI flag (config-file only, measured), so the project's own `node_modules` is the one place Core and the compiler resolve each other. Independently: TypeScript 7 breaks that preprocessor anyway with `ts.parseConfigFileTextToJson is not a function`, while 6.0.2 runs clean.
- [2026-08-13]: A scoped mutation run had an unscoped verdict. `--incremental` makes Stryker's report cumulative by design, and dharness judged the whole of it: measured, `dharness mutate src/a.ts:5-7` instrumented 5 mutants, then exited 1 naming a survivor at line 10 left over from an earlier whole-file run. The range only made it visible — the report accumulates across files too, so the same defect was reachable with bare paths from the day incremental was added. The tool reports project state; deciding what this run was asked about is dharness's job and nobody else's, which is the same boundary that made `SurvivorsError` necessary in the first place.
- [2026-08-13]: `filepath.ToSlash` is a no-op wherever the separator is already `/`, so it does not normalise anything on Linux — a path holding backslashes stays as it is. Comparing a scope path against a Stryker report key through it passed every Windows run and failed on ubuntu in CI, and the failure mode was the dangerous direction: no match means no survivors, which reads as a pass. The normaliser has to be an explicit `strings.ReplaceAll(path, "\\", "/")` when the input can carry either separator regardless of the host. Second half of the same lesson: `-race` could not be run locally to catch it, because this machine's `CC` points at an MSVC path containing spaces that cgo cannot parse and no gcc is installed — CI verifies something the author's machine cannot.
- [2026-08-13]: Correction to the line above, same day. gcc was genuinely absent when that was measured and was installed minutes later, so `-race` *is* runnable on this machine and the recorded limitation was already stale when it was written. What blocks it is `CC` alone — pointing at an MSVC path containing spaces, which cgo splits at the first one — and `CC=gcc go test ./... -race` passes every package. The narrower lesson is the one worth keeping: an environment limitation is a measurement with a shelf life, and writing it into an append-only log gives it a permanence the fact does not have. Re-check before recording that something *cannot* be done; "the code does this" ages far better than "this machine cannot".
- [2026-08-13]: `add` rewrites a dependency's version spec even when no tag is passed, so there is no way to "install what is already declared" with it — measured against a real repository, `bun add -d @stryker-mutator/core@latest` turned an exact `"9.6.1"` into `"^9.6.1"`. The command that respects a manifest is `install`, which reads it. The distinction matters most exactly where it is least visible: for a mutation engine a caret range means a minor can arrive on its own and change the verdict over code nobody touched, which is the reason the pin was exact in the first place.
- [2026-08-14]: `fallow audit` with no base flag audits the whole branch, not the staged change, and the comment claiming otherwise had been true only of the repository it was measured in. fallow's `--help` states the rule — with `--changed-since`/`--base` unset the base is "the git merge-base against the branch's upstream or the remote default" — and `--gate new-only` does not rescue it, because "new" is measured against that same base, so every commit the branch made since `main` counts as introduced. Measured against fallow 3.16.0 on a branch of 24 changed files with one clean file staged: bare `audit` reported "Audit scope: 25 changed files vs main (local main)" and exited 1 on 24 complexity findings and 8 clone groups that the staged change never touched. The 2026-08-09 measurement behind the old comment was taken in a repository with one changed file, where the branch's diff and the index are the same set — a fixture too small to distinguish the two answers, which is the part worth keeping: a measurement generalises no further than the thing that could have falsified it.
- [2026-08-14]: The fix is two flags and neither is sufficient alone, which is only visible in a fixture with a dirty working tree. `--changed-since HEAD` moves the base to the last commit, and measured, that alone restores the ratchet: a harmless edit inside a file already carrying debt passes, because against HEAD that debt is inherited rather than introduced. But it also reported a finding in a file that was only in the working tree and had never been staged — the working tree is not the index, and this gate's whole contract is the index. `--diff-stdin`, fed `git diff --cached`, is what holds that line. The recorded objection that `--changed-since` "cannot see the index" is false as measured on 3.16.0, which reported "Audit scope: 1 changed file vs HEAD" for a change that existed only there; what is true is that it needs a commit to point at, which `HasCommits` already guards.
- [2026-08-14]: The two failure modes of a diff filter are not symmetric, and only one of them is loud. Measured against fallow 3.16.0: a diff naming paths that match nothing is neither an error nor a rejection — the filter is ignored and scope falls back to whole files, so a repository-rooted path handed to a tool running in `frontend/` degrades the gate silently, which is why `StagedDiff` runs from Source with `--relative`. An *empty* diff does the opposite and worse: it admits nothing, and `audit` exits 0 over a genuinely bad staged file that the same run with a real diff exits 1 on. A green verdict from an empty scope is indistinguishable from a green verdict from a clean change, so `RunCheck` refuses an empty diff rather than passing it on.
- [2026-08-14]: fallow prints `--diff-file precedes --changed-since for line-level filtering; --changed-since still scopes file discovery. Drop one of them to disable this combination.` on every run that passes both, and its closing advice is exactly wrong here — dropping either flag reopens the hole the other covers. The notice is left unedited, because dharness reports what a tool says rather than rewriting it, and the reason not to follow it lives in `FallowAudit`'s comment where whoever reads the notice will look. It was also invisible to the suite: `go test ./...` was green before the binary was ever built, and the notice appeared on the first real run — the same lesson `structured-reports` already paid for.
- [2026-08-14]: One field serving three purposes let the gate state something false. `stage.tool` held the binary and was read by the section header, by the line naming what a failure skipped, and — as `runner.Command.Label` — by the failure message, so the two stages that both run `fallow` were indistinguishable in all three: an audit failure printed "fallow failed, so fallow did not run", a sentence about an event that did not occur, aimed at exactly the reader deciding what to run next. Splitting the display name from the binary fixed all three at once, because they were all reading the same field; the tool's own name stays on `stage.help`, whose sentence is about the tool rather than about one invocation of it. The gate had a test asserting the skip line said "fallow did not run" and it passed throughout — an assertion loose enough to accept the defect it was written to prevent.
- [2026-08-14]: Every conventional-layout adopter's ESLint config had been failing to load, and nothing in this repository could see it. `ownedFrom` returns `filepath.Rel`'s answer, which for a project config sitting beside `.dharness/` is `.dharness/eslint.config.js`; fallow's and lefthook's `extends` read that as the relative path it obviously is, and Node does not. An ES module's relative specifier must begin with `./` or `../`, so that string is a *bare* specifier — a package name — and measured against ESLint 9.39.5 on Node 22.19.0 the run dies with `ERR_INVALID_MODULE_SPECIFIER: Invalid module ".dharness/eslint.config.js" is not a valid package name`. Three renderers shared one path helper because all three emit a reference to the same file, and only one of the three consumers resolves it with Node. The split layout hid it: `../.dharness/...` is already a legal specifier, so the fixture that would have caught it is the ordinary one, not the exotic one.
- [2026-08-14]: `npx expo lint` generates CommonJS — `const { defineConfig } = require('eslint/config')` and `module.exports = defineConfig([...])` — and `jsconfig.Analyze` only knew `export_statement`, so it answered "no default export found" and dharness delegated. An Expo project, one of the two frameworks the registry has a preset for, therefore never got its layer wired at all, and the step reported that as a considered refusal rather than a gap. Worth keeping separately from the fix: the two frameworks disagree with each other, not with dharness — Next.js documents `eslint.config.mjs` with `import`/`export default` and Expo generates `.js` with `require`/`module.exports`, so "flat config" names a file's role and not its dialect, and anything that writes into one has to read which dialect it is looking at.
- [2026-08-14]: A CommonJS `require()` of an ES module returns the module *namespace*, not its default export, so `require("eslint-plugin-react-doctor").configs` is `undefined` and ESLint dies with `TypeError: Cannot read properties of undefined (reading 'recommended')`. The identical package reached by `import` is already the default export, which is why the Next.js sandbox passed and the Expo one did not — same registry entry, same generated expression, opposite results by dialect alone. Measured on Node 22.19.0: `eslint-plugin-react-doctor` 0.9.12 comes back with `Symbol.toStringTag === "Module"` and its configs under `.default`, while `eslint-config-expo/flat` 57.0.1 is a plain object with no tag, so the tag is the only thing that tells the two apart at runtime — a looser "does it have `.default`" test would unwrap a CommonJS module that merely exports a key by that name.
- [2026-08-14]: react-doctor's framework presets do not include `recommended`, and its documentation does not say either way. Measured against `eslint-plugin-react-doctor` 0.9.12: `recommended` carries 581 rules, `next` carries 25, `react-native` carries 40, and the overlap between `recommended` and either of them is **zero**. A preset contributing only `configs.next` ships 25 rules out of 606 while reading, in the generated config and in the plan output alike, as though react-doctor were fully wired. The doc sentence that settles it is one clause — "`recommended` contains the framework-independent rules" — and it describes what the preset *holds*, never what the others *include*; the composition question it appears to answer is the one it does not.
- [2026-08-14]: The framework flat configs live behind subpath exports, which splits one string into two facts: ESLint imports `eslint-config-next/core-web-vitals` and npm installs `eslint-config-next`. Passing the specifier to the package manager asks the registry for something that does not exist. The same split had a second edge — Next.js's page calls `core-web-vitals` "Recommended for most projects" and every flat-config example on it imports that subpath, while this repository had been contributing the bare package, which is the weaker base config, and citing that same page as its evidence. A `Because` that quotes a source is not yet a `Because` that quotes the sentence that decides.
- [2026-08-14]: The sandbox found three defects in one session that the Go suite, the goldens and `go vet` all passed over, and each one failed at ESLint startup rather than anywhere Go can reach: an unresolvable module specifier, a dialect mismatch, and an interop unwrap. The pattern is the same one `structured-reports` already paid for and it has a sharper statement now — when the deliverable is *generated code for another runtime*, the only test that can fail is running it in that runtime. `--print-config` is the cheap version of that check: it resolves the whole config and lets the plugin prefixes be counted, which is how "the layer is present" gets separated from "the file happened to be clean" (measured: 697 active rules for the Next.js fixture across `react-doctor`, `@next`, `@typescript-eslint`, `jsx-a11y` and `dharness`; 692 for the Expo one).
- [2026-08-15]: `os/exec` quotes a Windows argument only when it holds a space, a tab or a quote, which is the correct rule for `CreateProcess` and the wrong one for a `.cmd`, because cmd.exe parses the line a second time against a much larger set. Measured on go1.26.5 through a real shim: `(landing)` closed the shim's own `IF` block and the run died with `/page.tsx was unexpected at this time` and exit 255; `src/a&b/page.tsx` split the line and ran `b` as a command; `src/a^b/page.tsx` arrived as `src/ab/page.tsx` with the caret deleted and no error at all. A Next.js route group is enough to meet the first, and dharness reported all three as the wrapped tool's verdict, because from Go's side `Start` succeeded — cmd.exe started perfectly and then failed *as* eslint, which is the one thing `ExitError` and `StartError` exist to keep apart. The fix is `SysProcAttr.CmdLine` with every token quoted and the whole line wrapped again under `/s`, and `%` is refused rather than escaped: measured, `%VAR%` arrives as its value, `%%VAR%%` as the value still wrapped, `^%VAR^%` with the carets intact, so there is no encoding that survives and the only honest options are refusing or lying. The generalisable half is the fixture, not the escaping. `cmd-shim` — yarn, older npm — expands `%*` inside an `IF`/`ELSE` block and breaks; npm 10+ puts `%*` on the last line and does not. The first sandbox built for this used npm, reproduced nothing, and would have certified the bug as fixed before a line was written; yarn 4.9.1 on the same package reproduced the reported output verbatim. A sandbox is evidence only once it is shown to fail the way the report did — reproducing the *failure* is the measurement, and a green sandbox that was never red proves only that it is a different sandbox.
- [2026-08-15]: A defect fixed by hand three times is a defect nobody fixed. GitHub writes the pull request's title into every merge commit — the three combinations its API accepts for `merge_commit_title`/`merge_commit_message` are `PR_TITLE`+`PR_BODY`, `PR_TITLE`+`BLANK` and `MERGE_MESSAGE`+`PR_TITLE`, and a request for `MERGE_MESSAGE`+`BLANK` comes back 422, so no setting keeps a merge commit and leaves the title out. When that title is a conventional commit, release-please counts it twice and the changelog carries the entry against both the real commit and the merge. `f14eaa2` and `95858f3` are commits that exist only to delete those duplicates, from 1.0.1 and 1.1.0; 1.4.0 and 1.4.1 shipped with theirs still in, which is the measurement that matters — the manual fix was already the established practice and it failed the two times nobody was watching. release-please's config schema has thirty-four options and no way to ignore a merge commit, so the fix was to stop producing one: `allow_merge_commit` is off. The generalisable half is the diagnosis, not the setting — the repository's own history was the experiment, because the merges with an empty body never duplicated and every merge with a conventional body did, and that separated cause from coincidence without needing the tool's documentation to say so.
- [2026-08-20]: Two steps answering `true` to both `Satisfied` and `Delegated` is harmless in one caller and silent in the other, and the second caller arrived later. `Pending`/`applySteps` use `Satisfied` to select and `Delegated` to skip, so an eslint config shape dharness refuses to rewrite could answer both and still be explained. `run()` — the structured-reports rewrite — decides status in the other direction: `Satisfied` first, `continue`, `Delegated` never asked. The same pair that used to be a double report became a silent one, and a fresh `create-next-app` reported `= 6/11 point eslint.config.js at the file dharness owns / config shape not recognised` under **Already in place** with `dharness-eslint-plugin` and `eslint-plugin-react-doctor` freshly installed and wired to nothing, `8 satisfied · 0 failed · exit 0`. Three tests pinned the wrong behaviour, one of them named `...TypeScript_config_always_delegates_but_reports_satisfied`, because the rule they encoded was true of the caller that existed when they were written. A new consumer of an old predicate is a reason to re-derive its contract, not to trust the tests that already pass.
- [2026-08-20]: `lefthook install` exits 0 whether or not it installed anything. On a repository whose `lefthook.yml` declares no `pre-commit` job — including the all-commented scaffold `lefthook install` itself writes when it finds no config — it installs `prepare-commit-msg` alone, prints `sync hooks: ✔️`, and returns 0. dharness relayed that as proof the gate was armed and reported step 9 applied, three runs in a row, with no `pre-commit` hook on disk and `dharness check` never running. The direct signal was already in the file: `hookInstallStep.Satisfied` reads the hook. §09 has a sharper form here — the proxy and the direct signal can sit in the same type, and the bug is not choosing the proxy but never asking the question again after the subprocess answers. It cannot be a blanket re-check either: `installStep.Satisfied` deliberately answers "is there a JS project here", which is false immediately after a successful install, so the postcondition is an interface a step opts into.
- [2026-08-20]: Verifying a postcondition and *failing* on it are separate decisions, and only running the binary showed it. With the check in place, a project owning its own `lefthook.yml` — step 5 correctly delegated, "adding a key to it is a merge, not a write" — had step 9 run `lefthook install` anyway, install nothing, and fail the run: rolled back the install, the owned files, the ESLint layer and `.mcp.json` over a merge nobody had performed yet, taking step 5's own instructions down with it. Step 9 depends on step 5's outcome, so it delegates for the same reason instead. The check stays for what it was written for — a config that *does* carry the reference and a hook manager that still installs nothing. Making that dependency reachable also exposed a predicate that had been wrong and unobservable: `lefthookExtendsStep.Delegated` answered true for an already-wired config, harmless while only `run()` asked it (Satisfied won first) and wrong the moment another step did.
- [2026-08-20]: `Writer.remember` is called for two different reasons and `Changed` only knew one of them. `Write` remembers a file it is about to create, so absence-then-presence is certain; `installStep` remembers every lockfile the detected manager *might* rewrite, so a rollback can restore whichever one it picks. `Changed` reported `Created` for any snapshot marked `existed: false` without looking at disk, and bun's table holds both `bun.lockb` and `bun.lock`, so every sync on bun 1.4 reported `+ bun.lockb` for a file bun stopped writing in 1.2 — including runs where nothing changed. Both must stay in the snapshot set; what was wrong is reporting a prediction as an outcome. The reporter's second inference from the same symptom was false, and worth separating: step 1 is not kept in *applied* by the phantom entry, it is always applied by design, because `installStep.Satisfied` asks whether a JS project exists rather than whether the package is there.
- [2026-08-20]: An extension is a claim about a dialect, and `.js` makes it to whichever `package.json` is nearest. dharness wrote `.dharness/eslint.config.js` with ESM syntax into a Next.js project — a `package.json` with no `"type"` — so Node printed `MODULE_TYPELESS_PACKAGE_JSON` on every ESLint run, which for an adopted project is every gated commit, and Node's own suggested remedy (add `"type": "module"`) is a far larger change to the project than the warning warrants. The mirrored case was not noisy but broken: `module.exports` in a `.js` file under a `package.json` that *does* declare `"type": "module"` is a SyntaxError, and the Expo preset writes exactly that. Naming the file `.mjs` or `.cjs` by the dialect already computed answers both. Same session, same file, a second self-inflicted finding: `dharness/require-jsdoc` fired on the `export default function dharnessLayer(...)` dharness generates, in a file whose own header says every edit to it is lost on the next sync — a rule unsatisfiable by the only person it fires at. The layer ignores the directory dharness owns, which also covers whatever lands there later; the pattern is omitted in a split layout, where `.dharness/` is above ESLint's base path and a `../` pattern would be rejected rather than helpful.
- [2026-08-20]: `jsconfig` recognised `export default defineConfig([...])` and refused everything `create-next-app` emits, because the framework's scaffolder binds before it exports — `const eslintConfig = defineConfig([...]); export default eslintConfig;` — and the analyzer only ever looked at the node the export statement pointed straight at. One hop into a top-level declaration is the whole fix, and the refusal message named the call rather than the indirection, which is why the field report diagnosed `defineConfig` as unsupported when it had been supported all along. A refusal reason that names the wrong half of the shape sends whoever reads it to fix the wrong thing.
- [2026-08-20]: La decisión de resolver `react-doctor@latest` y `fallow@latest` en cada corrida se conserva (§03, enmienda del 10 de agosto), pero dejarla sin explicar era el defecto. Medido sobre un create-next-app de cinco archivos con cachés calientes: tres commits consecutivos tocando un archivo tardaron 23.1s, 23.0s y 22.6s, contra ~4.6s de análisis que las cuatro etapas reportan entre todas; el resto son tres viajes al registry. Un `git commit` de 23 segundos que no rinde cuentas de sí mismo se lee como que el análisis es lento, y el movimiento siguiente es `--no-verify` — así que el gate enuncia el trueque que está haciendo, antes de la espera y no después. Vale separar la evidencia del reporte de campo de su conclusión: la medición era correcta y las tres remedios propuestos contradecían una decisión medida que el reporte no podía ver.
- [2026-08-20]: Ignorar `.dharness/**` dentro de la capa cambió un mensaje por otro, y solo corriendo el binario se ve. El gate le nombra a ESLint cada archivo en stage de forma explícita, y ESLint avisa cuando un archivo que le pasaron está ignorado por su config: `File ignored because of a matching ignore pattern`. Así que commitear un cambio dentro de `.dharness/` imprimía un warning sobre una lista que armó dharness, no sobre nada que hiciera la persona. `--no-warn-ignored` es la respuesta de ESLint en su propio `--help` — "Suppress warnings when the file list includes ignored files" — y no suprime nada accionable, porque esa lista no es del usuario. La suite estaba verde en las dos formas del bug: el warning vive en la salida de un subproceso que ningún test de Go ejecuta.
- [2026-08-20]: Enseñarle a `jsconfig` la forma de `create-next-app` convirtió un paso delegado en un archivo editado, y con eso heredó la duplicación que el reporte de campo ya había anticipado: el scaffolder spreadea `eslint-config-next/core-web-vitals` y `eslint-config-next/typescript` por su cuenta, el preset de Next aporta los mismos dos, y el resultado cableado importaba cada uno bajo un segundo nombre y lo spreadeaba dos veces. Borrar los spreads del proyecto es la alternativa obvia y es la equivocada: esos bytes están fuera de las regiones marcadas, y el §03 hace de esas regiones los únicos bytes que esta herramienta toca. Lo que dharness decide es lo que dharness *aporta*, así que eso es lo que cambia — `contributedLayers` descarta la capa cuya especificación el config del proyecto ya trae. Dos detalles que no son opcionales: la comparación va por especificación y nunca por paquete, porque `eslint-config-next` y `eslint-config-next/core-web-vitals` son configs distintos detrás de subpath exports distintos y el pelado es el más débil; y hay que excluir la propia región de imports de dharness antes de preguntar, porque importa todo lo que aporta y contarla haría que la segunda corrida descartara todas las capas y la tercera las volviera a agregar. Verificado corriendo: el fixture de Expo en CommonJS ya requería `eslint-config-expo/flat`, así que su golden perdió la capa — la misma duplicación estaba ahí desde antes y nadie la había visto.
- [2026-08-20]: C3 se cierra con una medición, no con una opinión. `react-doctor@0.9.12 --help` no expone ningún flag que calle el bloque "Agent guidance" ni el pie de marketing: lo más cercano es `--no-warnings`, que además esconde hallazgos reales, y `--json`, documentado como "suppresses other output". Tomar `--json` convertiría a dharness en el renderizador de los hallazgos de react-doctor en lugar de la cosa que lo corre, que es exactamente la frontera que el §03 dibuja y la misma por la que el aviso de fallow se relaya sin editar. Filtrar el bloque del stream es el mismo trueque con otro sombrero. Queda como está y el pedido es upstream. Nota al margen para quien pase por acá: el `--help` de 0.9.12 sí lista `--blocking <level>`, que el comentario de `ReactDoctorStaged` describe como rechazado por el binario publicado — eso se midió contra 0.9.11 y puede haber cambiado.
- [2026-08-21]: `dharness mutate` scoped its verdict and left the table beside it cumulative, and the two disagreeing on screen cost a reader an afternoon of debugging tests that were never broken. `SurvivorsInScope` already existed and its comment already said why — "the report is cumulative and the verdict must not be", measured 13 August — so the half that was missing was never the reasoning, it was that the *table* is Stryker's own clear-text reporter over the same cumulative report and dharness had no sentence about it. Measured on a bun/vitest fixture with three files: naming one clean file printed `All files ... 4 survived` two lines above `Every mutant was caught`, exit 0. Both true of different things, which is exactly what makes it invisible. No Stryker option scopes that table to `--mutate` — `clearTextReporter.skipFull` skips fully covered files, not out-of-scope ones — so the fix is not to rewrite the table (§03) but to say what it is, and only when the report really does carry files the run never named. The second half is that `.git/dharness/stryker-incremental.json` is dharness's own choice, not Stryker's default of `reports/`: the report author found it with `find . -iname '*stryker*'` because nothing in the output, the help text or the report path mentioned it. A location chosen for good reasons still has to be disclosed by whoever chose it.
- [2026-08-21]: Two guesses in a row about why a path printed in full, and the measurement killed both. The suspicion was mixed separators — `dir` from the process in backslashes, the target from `git rev-parse` in forward slashes — and the fix looked obvious enough to write a comment justifying it. `filepath.Rel(`+"`C:\a\b`"+`, "C:/a/b/.git/x.json")` returns `.git\x.json` with a nil error, because Windows treats both characters as separators; on Linux neither side produces a backslash, so there is no mix to normalise anywhere. The real cause was that the helper was never called: an earlier edit had left the call site pointing at the old expression, so the new function was dead code that compiled. A comment explaining a mechanism that was never the mechanism is worse than no comment, because the next reader trusts it.
- [2026-08-21]: The field report asked for a `// dharness-ignore-next-line mutant <reason>` marker so a team could record that a surviving mutant is equivalent and keep the build meaningful — its argument being that an unclearable red build is one people stop running. Stryker already ships exactly that, reason and all: `// Stryker disable next-line all: <reason>`, and the general form `// Stryker [disable|restore] [next-line] *mutatorList*[: reason]`. Measured rather than read off the docs, because what mattered was the verdict and not the report: the directive moved four mutants in that file to `Ignored`, took survivors from 2 to 0, and `dharness mutate` exited 0. That is the whole of the request, including the exit code, which was the part actually blocking anyone. So the answer was documentation, not a marker — CLAUDE.md's first rule, arriving as a feature request dressed in a reasonable justification. The rule's test is not "is this a good idea", it is "did the wrapped tool already do it", and the only way to answer that is to go read its `--help` and its docs before designing anything.
- [2026-08-21]: The local gate ran `gofmt -l $staged` on staged *paths* and then read whatever the working tree held at them, and ran `go vet ./...` and `go test ./...` against the working tree without consulting the index at all. On a whole-tree commit those are the same bytes and nothing looks wrong. On a partial-index commit — every commit in a change split into work units — they are different trees, and the hook was reporting on the one not being committed. Audited commit by commit in isolated worktrees: ten consecutive commits reached main failing `gofmt -l .`, every one of them committed while the working tree was already correct, so a working-tree check had nothing to report. `git checkout-index -a --prefix=` is what makes it honest, and the counterfactual is the measurement worth keeping: on a fixture whose working tree is clean and whose index is not, the previous hook exits 0 and the current one exits 1. The build cache is content-keyed and global, so materialising the index costs almost nothing on a second run.
- [2026-08-21]: Verifying every commit of a branch and then rebasing verifies nothing that reaches main. The rebase rewrites each SHA, CI runs on the merge result rather than per commit, and the branch that was checked no longer exists. Those ten commits were each built and tested in an isolated worktree before the rebase — the check worked, it was simply run on objects that were then discarded. The generalisable half is that a verification is bound to the object it examined, so anything that rewrites objects invalidates it, and the only verification that counts is the one run on what actually lands.
- [2026-08-29]: El preset de Expo aportó durante cuatro versiones un config de ESLint que no carga, y las dos causas venían del mismo lugar: creímos la guía del framework en vez de medir el paquete. `eslint-config-expo` publica a la vez un directorio `flat/` y un archivo `flat.js`, y no declara `exports`; CommonJS resuelve el archivo y Node ESM rechaza el directorio con `ERR_UNSUPPORTED_DIR_IMPORT`, así que el subpath pelado que la guía de Expo escribe solo funciona en el dialecto que la guía usa — y todo config que dharness escribe desde cero es ESM. Medido en 57.0.2 / Node 22.19.0: `require("…/flat")` y `require("…/flat.js")` devuelven la misma instancia, con lo cual la extensión no le cuesta nada al camino CommonJS y es la única grafia que sirve en los dos. La segunda es peor de leer: ese subpath exporta un **array de 13 configs**, no un objeto, y el ejemplo de Expo lo incluye sin spread — correcto ahí únicamente porque el ejemplo envuelve todo en `defineConfig()`, que aplana arrays anidados. La fábrica que dharness escribe devuelve un array pelado, así que el mismo elemento llega a ESLint como `TypeError: Unexpected array`. La forma documentada de un config no es su forma real: lo que hay que copiar del ejemplo es el valor medido, no la sintaxis. Nada de esto lo ve Go — el import resuelve, el registry valida, los goldens comparan texto contra texto y las cuatro versiones pasaron verdes; lo que lo probó fue construir el binario, correr `sync` en un proyecto de scratch y ejecutar `eslint` (exit 2 antes, exit 1 — un hallazgo de lint real — después). Y un efecto de borde que casi se cuela: `contributedLayers` compara por especificación, así que agregar `.js` habría dejado de reconocer el `require("eslint-config-expo/flat")` que genera `npx expo lint` y habría vuelto a agregar el config entero en un proyecto que ya lo tenía — la comparación pliega ese sufijo, y solo ese, porque `.js` es la única extensión que `require()` agrega por su cuenta.
- [2026-08-29] (segunda): `sync` salía 0 con un config de ESLint que no cargaba, y así dos releases seguidas imprimieron exactamente la misma línea limpia: 1.7.0 con tres defectos y 1.7.1 con uno. Nada en la salida distinguía "el config que escribí funciona" de "el config que escribí no carga", así que la herramienta no podía avisarle al usuario ni a sus propios mantenedores que había regresionado. El arreglo es un comando que ESLint ya trae y un veredicto que es un exit code: `eslint --print-config`. Tres cosas que la medición cambió respecto del diseño obvio. (1) **La redefinición de un plugin es por archivo, no por config.** Dos objetos que registran `react-doctor` bajo `files: ["**/*.ts"]` y `["**/*.tsx"]` resuelven limpio; los mismos dos bajo un solo matcher salen 2. Así que una sonda sobre un solo archivo aprueba un proyecto cuya colisión está en otra extensión — se sondea una ruta por extensión distinta del árbol, acotado a ocho por `project.IsSourceFile`. (2) **El archivo sondeado no necesita existir**, y una ruta ignorada también sale 0: `--print-config` resuelve el config, no lee el archivo. Por eso el piso es el propio config del proyecto y un proyecto sin fuentes igual se verifica. (3) **No alcanzaba con `Verify`.** Un proyecto ya cableado por un release anterior tiene el config byte-idéntico, un paso satisfecho nunca se aplica, y un paso que no se aplica nunca se verifica — el release que arregla esto habría impreso lo mismo que el que lo rompió, justo en las máquinas que tienen el problema. La condición vive en `Satisfied` **y** en `Verify`, detrás de la comparación de bytes (§12). Precedente: `boundariesOwnerStep.Satisfied` ya lanza dos subprocesos de fallow. Costo medido de la sonda, mismo fixture convergido, dos corridas cada uno: 1.69/1.84 s con 1.7.1 contra 4.06/4.02 s con la sonda — +2.3 s por tres sondas (`.js`, `.ts`, y el `.mjs` del config). Se paga en `sync`, nunca en el gate. Y una que solo apareció corriendo el binario: la nota del gate que separa un error de configuración de un hallazgo de lint miraba `command.Name`, que `tool.Installed` llena con la ruta resuelta del binario mientras el nombre de la herramienta va en `Label`. El test unitario construía un `stage` a mano y pasaba; el gate real nunca imprimía la nota. El test ahora construye el stage con `localStage`, igual que el producto.
- [2026-08-29] (tercera): la colisión de `react-doctor` entre dharness y `dlinter-ts-react` no es un choque de nombres, es un **conflicto de versiones disfrazado de uno**, y eso invierte cuál es el arreglo. Medido por el proyecto que la reporta, contra su instalación real: dlinter empaqueta react-doctor **0.7.4 (395 reglas)** y dharness instala el standalone **0.9.12 (884 reglas)**; hoy dlinter ya deja 231 reglas `react-doctor/*` activas por su cuenta, así que la pregunta "¿ya está cubierto?" contesta que sí — y la conclusión obvia sigue siendo falsa: de las 621 reglas únicas que aportan los dos configs de dharness (`recommended` 581 + `react-native` 40), **400 no están activas con dlinter**, incluido el preset `react-native` entero, que en un código Expo/RN es justo el de mayor valor. Detectar-y-omitir habría bajado silenciosamente a ese proyecto de 884 reglas a 395 sin ninguna señal, que es exactamente el no-op silencioso que este repositorio corrige una y otra vez. El atajo natural también murió medido: aportar solo las `rules` sin una segunda clave `plugins`, apoyándose en el registro de dlinter, falla con `Could not find "rn-bottom-sheet-no-ignored-scroll-prop" in plugin "react-doctor"` — la instancia 0.7.4 no contiene esas implementaciones. Lo que queda son dos caminos con costo real (un opt-out explícito, o registrar bajo un nombre distinto y aceptar que cambian los IDs de regla, los `eslint-disable` existentes y el CI que los grepea), y ninguno es una decisión que una heurística deba tomar sola. Por eso 1.7.2 se queda en rechazar en vez de romper: el paso falla nombrando el error de ESLint, hace rollback, y el proyecto conserva su config que sí carga.
- [2026-08-29] (cuarta): el defecto 03 sí se podía resolver por construcción, y la señal directa la trajo la misma sonda que arregló el hueco de auto-verificación. `eslint --print-config` devuelve un array `plugins` con entradas `clave:nombre@versión` — `["@", "import:eslint-plugin-import@2.32.0", "react-doctor:react-doctor@0.9.12", …]` — así que qué plugins registra el config de un proyecto es **legible**, y ya no hay que inventar una tabla de "paquetes que traen react-doctor", que es la señal proxy que el §09 prohíbe. Lo único que se lee es la clave antes del primer `:`, porque la clave es lo que flat config se niega a ver dos veces; el nombre y la versión de atrás son otra pregunta. El problema real no era detectar sino **no oscilar**: si dharness retira su capa porque el plugin ya está registrado, en la corrida siguiente el plugin sigue registrado — a veces por el proyecto, a veces por la propia dharness — y una regla ingenua agrega y quita en corridas alternas para siempre. La respuesta sale de leer la **propia región de imports de dharness**, que ya dice qué aportó la vez pasada: se retira solo si el plugin está registrado **y** la región de dharness no lo importa. Tres estados y los tres estables al repetirse — lo trae el proyecto (retirar, y seguir retirando), lo trae dharness y el config igual resuelve (no hay colisión, aportar), o no está registrado (aportar). Un config que no carga no reporta plugins, así que ahí no se retira nada y habla el chequeo del paso, que es el orden correcto: retirar sobre un config que nadie pudo resolver sería adivinar cuál registro sobraba. Lo que **no** se hizo, y por qué. Renombrar la clave del plugin (`dharness-react-doctor/*`) conservaría las 884 reglas, pero exige generar JavaScript que reescriba el config de otro paquete — sus `plugins`, sus IDs de regla, y cualquier `settings` o processor que los referencie — y eso es dharness inventando semántica ajena, que es justo la primera regla del CLAUDE.md, con modos de falla que ningún test en Go ve. Y un flag de opt-out es superficie de configuración nueva para una pregunta que el proyecto ya contesta en su propio config. La capa retirada **se reporta en cada corrida** como nota accionable con el costo nombrado: eso es lo que la separa de detectar-y-omitir en silencio, que habría bajado ese proyecto de 884 reglas a 395 sin decir nada. Verificado corriendo el binario: sync pasa de exit 1 a exit 0 con 5 pasos aplicados, ESLint pasa de exit 2 a exit 1 (hallazgo real), y tres corridas seguidas dejan los mismos bytes.
- [2026-08-29] (quinta): la retirada de capas de 1.7.3 no funcionaba en el repo real, y la causa es que **el detector y el verificador preguntaban por archivos distintos**. `registeredPlugins` sondeaba un solo archivo — el propio flat config del proyecto — mientras `eslintExtendsStep` sondea una ruta por extensión. Y ya estaba medido en este mismo repositorio que **la redefinición de un plugin es por archivo**: `dlinter-ts-react` registra react-doctor bajo `files: ["**/*.ts", "**/*.tsx"]`, así que no aparece en el config resuelto para el `eslint.config.mjs` de al lado. Reproducido: `--print-config eslint.config.mjs` devuelve `["@"]` y sobre un `.tsx` devuelve `["@", "react-doctor:react-doctor@0.7.4"]`. El detector veía cero registros, aportaba la capa, y el verificador — que sí pregunta por extensión — encontraba la colisión de la que al detector le acababan de avisar en otra forma. **Escribimos un detector que ignoraba nuestra propia medición, tomada tres horas antes.** El fixture no lo detectó porque su dlinter falso registraba el plugin **global**, sin `files`, y entonces aparece para cualquier ruta — la misma trampa que ya está anotada para el fixture de npm que no reproducía el bug de yarn: un sandbox que no copia la *forma* del caso real solo prueba que el sandbox está de acuerdo consigo mismo. El fixture ahora registra bajo `files` y falla primero con 1.7.3. La regla general que queda: **cuando dos partes del código le preguntan lo mismo a la misma herramienta, tienen que preguntarlo sobre el mismo conjunto de entradas**, o una de las dos contesta sobre un mundo que la otra no ve. Aquí el detector ahora recorre `eslintProbePaths` y une las claves; el memo sigue separado del verificador a propósito, porque el archivo cambia entre las dos preguntas y compartirlo devolvería la respuesta previa al cableado. Aparte, dato útil: el índice del proxy de módulos de Go tarda en levantar un tag nuevo. Justo después de publicar 1.7.3, `go install …@latest` seguía trayendo 1.7.2 y `go list -m -versions` cortaba en v1.7.2; minutos después ya resolvía v1.7.3. Al avisar de un release recién publicado conviene nombrar la versión explícita (`@v1.7.4`) en vez de `@latest`.
- [2026-08-29] (sexta): la nota de retirada describía la brecha en abstracto — "esa copia no es necesariamente el mismo build: si es una más vieja, las reglas que le faltan simplemente no se aplican" — **mientras tenía los dos números concretos en la mano**. El build residente sale de la misma entrada `plugins` con la que se decidió la retirada (`react-doctor:react-doctor@0.7.4`), y el de dharness se lee del `package.json` desempaquetado en `node_modules` (0.9.12). Un condicional le pide al lector que primero sospeche que hay una brecha y después vaya a medirla; un hecho se acciona. Ahora la nota **abre** con él: «la copia de "react-doctor" acá es 0.7.4, y dharness instala 0.9.12 — así que toda regla que agrega el build más nuevo no se está aplicando en este proyecto, y nada más te lo va a decir». Va primero porque es la oración que carga el peso: enterrarla detrás del mecanismo es lo que hace que una nota se lea en diagonal. Medido antes de escribirla, para no comparar dos cosas distintas: la versión del paquete instalado y la que ESLint reporta para el plugin que registra son la misma cadena (`react-doctor:react-doctor@0.9.12` junto a un package.json que dice 0.9.12). Son hechos de dos fuentes distintas, así que se etiquetan por fuente y no se funden. Si falta cualquiera de los dos números vuelve la forma condicional: imprimir «0.7.4 contra » con un blanco sería peor que el hedge que reemplaza. Y si los dos builds coinciden no hay brecha, así que no se anuncia ninguna — anunciar una que no existe es cómo el lector aprende a dejar de leer la nota. También se cayó el diagnóstico que sugería: `npx eslint --print-config <archivo>` lista las claves de regla bajo el prefijo, pero saber cuáles *faltan* exige diferenciar esa lista contra el set de reglas de 0.9.12, que es la mitad difícil y quedaba sin decir. Las dos versiones son la misma información en una forma que nadie tiene que calcular. Y una decisión explicitada en el código en vez de quedar por omisión: la nota se imprime en **cada** corrida, incluido un segundo sync idéntico. Una nota que se muestra una sola vez es una nota que se pierde quien corre el segundo sync, y lo que está cuidando es un set de reglas encogiéndose en silencio — exactamente el fallo que pasa inadvertido durante releases enteros.
